name: fan_context_menu
description: "A long-press fan-shaped action menu: press and hold, slide to an action, release to execute."
version: 0.1.1
repository: https://github.com/4armsxlr8/fan_context_menu
issue_tracker: https://github.com/4armsxlr8/fan_context_menu/issues

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

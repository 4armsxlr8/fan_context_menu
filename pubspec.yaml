name: pinterest_long_press_menu
description: "Pinterest風の長押しメニュー(長押し→スライド→離して実行)を再現するFlutterサンプル。"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
